# Task

## Objective

**Goal:**
- When user request a return, it will create another request like it is duplicated

**Expected outcome:**
- it should get the id of that item, find in the database then update the status to "waiting for custodian approval instead of displaying another reqest 

## Context

**Current behavior:**
- [What currently happens?]

**Problem:**
- [What is wrong?]

**Desired behavior:**
- [What should happen instead?]

## Relevant Documentation

Read only the documentation relevant to this task:

- `docs/architecture.md`
- `docs/roles-and-access.md`
- `docs/inventory-workflows.md`
- `docs/database.md`
- `docs/ai-assistant.md`
- `docs/testing.md`
- `docs/deployment.md`

## Scope

**Inspect:**
- `[file/module]`
- `[file/module]`

Trace related routes, controllers, requests, models, services,
policies/middleware, and tests as needed.

**Modify:**
- `[file/module]`

Do not modify unrelated functionality.

## Requirements

1. [Requirement]
2. [Requirement]
3. [Requirement]

## Business Rules

- Preserve role-based access.
- Maintain inventory audit trails.
- Do not bypass approval workflows.
- Keep stock tracking consistent.

## Security

- Verify authentication and authorization.
- Verify resource ownership.
- Check for IDOR and parameter tampering.
- Validate all relevant inputs.
- Never trust client-supplied IDs.
- Authorize before sensitive state changes.
- Preserve transaction integrity.

## Implementation Rules

- Follow `.github/copilot-instructions.md`.
- Follow the existing architecture.
- Make the smallest safe change.
- Reuse existing code where appropriate.
- Do not introduce unnecessary dependencies.
- Do not change the database schema unless required.
- Do not modify unrelated functionality.

## Testing

- Inspect existing tests first.
- Add/update tests for changed behavior.
- Add regression tests for bugs/security issues.
- Test successful and rejected/unauthorized scenarios.

Recommend command:

```bash
php artisan test tests/Feature/[TestFile].php
php artisan test